export default defineAppConfig({
    ui: {
        primary: 'customPrimary',
        button: { default: { size: 'lg', color: 'primary', textColor: 'white' } },
        divider: { border: { base: 'border-olive-600' } },
    },
});
